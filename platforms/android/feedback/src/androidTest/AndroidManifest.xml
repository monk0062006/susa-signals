<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!--
          Test-only host for ScreenCapture. Declared here rather than in the
          library manifest so it never merges into a consumer's app.
        -->
        <activity
            android:name="io.markerusa.feedback.CaptureTestActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <activity
            android:name="io.markerusa.feedback.MaskingTestActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <activity
            android:name="io.markerusa.feedback.SurveyTestActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />
    </application>

</manifest>
